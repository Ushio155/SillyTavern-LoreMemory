# AI 开发声明

- **设计与取舍由作者完成**：产品定位（不重写检索、只做"内容生产线 + 仪表盘"）、
  数据结构、字段契约、节流策略、失败与降级矩阵、以及全部源码级结论，均来自作者对
  SillyTavern 1.18.0 源码的阅读与判断。
- **AI 辅助实现与验证**：代码编写、测试套件、文档整理与发布流程由 AI 编程助手协助完成。
- **本仓库不包含任何第三方运行时依赖**：`src/` 下全部为零依赖纯函数，
  插件本体只 import SillyTavern 自身的模块。

## 关于"记忆"这件事的诚实说明

这个插件的召回能力**受限于 SillyTavern 世界书机制本身**：
关键词只扫描最近 N 条消息（全局默认仅 2 条）。

> 它保证的是「**被再次提及过的**旧细节能被召回」，
> 不保证「任何旧细节都能召回」。

这是设计选择，不是缺陷；想要"没被提及也能召回"需要向量检索，属于未实现的 P2。

## 关于成本

「自动记忆」开启后，每满 N 条消息会**真实调用你所配置的 API**（消耗 token）。
因此它默认是**关闭**的。省下的是**输入** token，花掉的是**摘要生成** token ——
净收益通常为正，但这不是零成本。
